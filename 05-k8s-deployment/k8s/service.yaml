apiVersion: v1
kind: Service
metadata:
  name: prod-service

spec:
  type: NodePort

  selector:
    app: prod-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007